// Person details
let weight = 50; // in kg
let height = 5.6; // in feet
let age = 25; // in years

// Roller Coaster eligibility check
if (
  weight > 60 &&
  weight < 120 &&
  height > 4.5 &&
  height < 7 &&
  age > 20 &&
  age < 60
) {
  console.log("🎢 You are eligible to ride the Roller Coaster!");
} else {
  console.log("❌ Sorry, you are not eligible for the Roller Coaster ride.");
}
